# api/index.js is a generated, self-contained esbuild bundle of
# server/_core/serverless.ts (produced by `pnpm run build:vercel` and
# regenerated by Vercel on every deploy). Mark it generated so GitHub
# collapses it in diffs and excludes it from language stats.
api/index.js linguist-generated=true -diff
